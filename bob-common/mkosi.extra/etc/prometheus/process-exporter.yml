process_names:
  # Monitor the searcher container cgroup
  - name: "{{.Matches}}"
    cmdline:
    - 'podman'

  # Monitor the container init process
  - name: "searcher-container"
    cmdline:
    - 'catatonit'
