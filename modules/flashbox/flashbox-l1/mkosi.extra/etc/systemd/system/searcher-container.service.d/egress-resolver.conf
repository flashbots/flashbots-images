[Unit]
# The container's /etc/hosts is a symlink into /run/flashbox-endpoints, which
# egress-resolver populates. Wants= (not Requires=) so a resolver failure never
# takes the container down; production stays refused by toggle instead.
Wants=egress-resolver.service
After=egress-resolver.service
