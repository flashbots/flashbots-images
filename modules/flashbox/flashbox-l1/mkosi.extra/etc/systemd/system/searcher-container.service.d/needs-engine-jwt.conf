# The container bind-mounts /tmp/jwt.hex and /persistent/sync_proxy_logs; both
# must exist before podman starts (otherwise podman fails or creates a directory
# in place of the file).
[Unit]
After=engine-jwt.service sync-proxy-init.service
Requires=engine-jwt.service sync-proxy-init.service
